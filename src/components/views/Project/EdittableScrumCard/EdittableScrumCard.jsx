import React from 'react';
import styles from './EdittableScrumCard.module.scss';

export const EdittableScrumCard = (props) => {
  const {
    card
  } = props;
  
  return (
    <div>

    </div>
  )
}